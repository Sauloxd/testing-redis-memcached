config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Simple Load test
      
scenarios:
  - name: "Testing Redis with single command"
    flow:
      - get:
          url: "/redis/charge_request_single"
  