config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Simple Load test
      
scenarios:
  - name: "Testing Redis with multiple commands"
    flow:
      - get:
          url: "/redis/charge_request"
